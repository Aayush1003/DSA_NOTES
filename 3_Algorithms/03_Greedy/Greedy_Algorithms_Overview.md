# Greedy Algorithms

A Greedy Algorithm builds up a mathematical solution piece by piece. At every immediate step, it definitively chooses the **locally optimal** (most immediately beneficial) option, completely ignoring fundamentally future consequences, hoping that these local optimums mathematically map cleanly to a **global optimum** solution.

## 1. When do Greedy Algorithms Work?

To mathematically assure a Greedy solution physically resolves strictly perfectly, the problem must exhibit purely two properties:
1. **Greedy Choice Property**: A structurally mathematically global optimal solution strictly physically can be arrived at by selecting purely local immediate mathematical optimums explicitly.
2. **Optimal Substructure**: An optimal logically mathematical solution natively explicitly contains structurally optimal structurally bound solutions physically to purely mapping subproblems.

*Warning*: If these strictly structurally fail computationally, Greedy mathematically returns purely exclusively sub-optimal (wrong) outputs computationally. In strictly mathematically bound cases inherently mapping where Greedy structurally severely naturally fails rigidly implicitly, **Dynamic Programming** logically inherently maps fundamentally structural safe bounds.

## 2. Common Greedy Algorithm Approaches

### 1. Activity Selection / Interval Scheduling
- **Setup**: You structurally map naturally rigidly multiple logically computing activities physically strictly bound by internal internal Start & End internally explicitly mapped times. You mathematically cleanly want strictly purely computing maximum internally explicitly mathematically non-overlapping activities.
- **Greedy Logic**: Sort purely natively purely fundamentally activities cleanly purely bounded structurally strictly by their **End Times**. Repeatedly physically pick strictly the formally formally logically earliest ending natively mapping activity computationally rigidly mathematically explicitly structurally natively avoiding overlaps.

### 2. Fractional Knapsack bounds
- **Setup**: You natively completely rigidly have computationally mathematically completely internal purely bound items with physical weights internally mapped rigorously cleanly to computationally rigidly map strictly fundamentally explicit purely mapped formally logically Values. You mathematically purely can intrinsically structurally physically split explicitly internally mathematical natively items mathematically strictly to maximize logically mapped structural bounds.
- **Greedy Logic**: Calculate rigorously firmly purely mathematically computationally rigorously internally intrinsically formal Value/Weight mapped naturally rigidly mapping structurally explicit ratio mathematically. Sort structurally mathematically items purely mapping intrinsically fundamentally purely cleanly purely descending entirely based structurally rigidly purely on naturally mathematically entirely logical ratio cleanly. 

### 3. Huffman Coding
- **Setup**: Data Compression computationally internally rigorously cleanly mapping fundamentally computationally purely purely bounded frequencies.
- **Greedy Logic**: Always mathematically structurally merge internal mapping logically bounds explicitly completely mapping the rigorously cleanly internally fundamentally structurally two computationally rigorously mathematically lowest purely cleanly strictly mathematically completely purely frequencies rigidly bounded explicitly computational mapping mathematically natively purely explicit mathematically bounds natively explicitly.

### 4. Graph Greedy Paradigms
- **Dijkstra's Algorithm**: Always strictly structurally formally logically formally purely strictly completely visits cleanly strictly mathematically fundamentally bounded physically the closest unvisited purely completely logically bounded node inherently mathematically.
- **Kruskal's / Prim's Algorithm**: Always computationally mapping purely mathematically fundamentally adds strictly strictly mapped explicitly purely computationally the lowest rigorously completely bounded logically mapping legally structurally physically explicit computationally edge.

## Deep Thought: Greedy vs Dynamic Programming
- **Greedy**: "I will take explicitly computationally strictly physically the biggest rigorously mathematically logically bound formal formally purely slice intrinsically completely mathematically bounded of fundamentally pizza cleanly structurally formally purely rigorously right now natively." 
- **DP**: "I computationally natively formally rigidly analyze mathematically all structurally formally logically mapped formally formal completely internally physical formally future slices entirely intrinsically structurally formally before rigorously eating entirely natively completely mathematically logically."
